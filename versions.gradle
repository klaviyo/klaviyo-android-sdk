ext {
    // build dependencies - not managed by refreshVersions
    dokkaVersion = '1.7.20'
    gradleKtlintVersion = "11.3.1"

    // android versioning
    minSDKVersion = 23
    targetSDKVersion = 34
    androidCompileVersion = targetSDKVersion
    buildToolsVersion = '34.0.0'

    // project versioning
    versionCode = 10
    versionName = '2.0.0-alpha.1' // Reminder: Update version in README samples and update documentation branch
}
