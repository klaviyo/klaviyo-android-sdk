<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application>
        <service
            android:name="com.klaviyo.pushFcm.KlaviyoPushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- This is an opt-in behavior for now, likely to become the default behavior in next major release -->
        <meta-data android:name="com.klaviyo.push.automatic_open_tracking" android:value="false"/>

    </application>
</manifest>
